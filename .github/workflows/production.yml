name: Production Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read
  pull-requests: write

jobs:
  validate:
    name: Validate & Type Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Code Quality (Lint)
        run: pnpm lint

      - name: Consistency (Format)
        run: pnpm format:check

      - name: Type Integrity (Tsc)
        run: pnpm tsc --noEmit

  security:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Dependency Audit
        run: pnpm audit --audit-level=high

  build:
    name: Production Build
    needs: [validate, security]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Application
        run: pnpm build
        env:
          # Production environment variables (Mocked for CI build phase)
          DATABASE_URL: postgresql://ci_user:ci_password@localhost:5432/ci_db
          AUTH_SECRET: ${{ secrets.AUTH_SECRET || 'ci_secret_placeholder' }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY || 'ci_gemini_placeholder' }}
          NODE_ENV: production
          NEXT_PUBLIC_APP_URL: https://memo-lane.vercel.app

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: production-build
          path: .next
          retention-days: 5
