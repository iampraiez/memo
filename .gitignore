node_modules
.env.production
.env.development

.env
.next

.next/

scripts/

# next-pwa generates these fresh on every `next build`.
# Committing them locks in stale chunk hashes → breaks production precaching. (Rule F)
public/sw.js
public/workbox-*.js
